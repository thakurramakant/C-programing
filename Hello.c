# include<stdio.h>

int main(){
    float len;
    printf("Enter the radius of the circle: ");
    scanf("%f",&len);

    
    printf("Area of the circle of the given radius is: %f ",len*len*3.14);
    return 0;
}